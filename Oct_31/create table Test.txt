create table Test
(
    AccountNO INT Primary Key
);

-- Write a query to generate accountNo automatically, as if no records accountNO is 1, else last accountNo + 1

select case when max(accountNo) IS NULL THEN 1 else max(accountNo)+1 end
'Accno' from Test;

Create Table Users
(  
   UserName varchar(30) Primary Key, passcode varchar(30) NOT NULL
);

Insert into Users(username,passcode) values('Revathi','Codegnan'),
('Venkat','Venkatrao');

select count(*) cnt from Users where userName='Revathi' AND Passcode='Codegnan';





